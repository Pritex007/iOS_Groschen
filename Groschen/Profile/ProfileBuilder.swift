import UIKit

// TODO: Implement Profile module (GROS-14)

final class ProfileBuilder {
    func build(coordinator: CoordinatorProtocol) -> UIViewController {
        let viewController = ProfileViewController()
        return viewController
    }
}
